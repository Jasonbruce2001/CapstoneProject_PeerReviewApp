@model AddAssignmentVM
@{
    ViewData["Title"] = "Add Assignment";
}

<h1 class="text-center fw-bolder">Add Assignment</h1>
<h4 class="text-center">@Model.ClassName - @Model.Term</h4>

<div class="row">
    <div class="col-md-6 offset-md-3">
        <form asp-action="AddAssignment" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ClassId" />
            <input type="hidden" asp-for="ClassName" />
            <input type="hidden" asp-for="Term" />

            <div class="form-group mb-3">
                <label asp-for="Title" class="control-label">Assignment Title</label>
                <input asp-for="Title" class="form-control" />
                <span asp-validation-for="Title" class="text-danger"></span>
            </div>

            <div class="form-group mb-3">
                <label asp-for="DueDate" class="control-label">Due Date</label>
                <input asp-for="DueDate" class="form-control" type="date" />
                <span asp-validation-for="DueDate" class="text-danger"></span>
            </div>

            <div class="form-group mb-3">
                <label asp-for="ReviewDueDate" class="control-label">Review Due Date</label>
                <input asp-for="ReviewDueDate" class="form-control" type="date" />
                <span asp-validation-for="ReviewDueDate" class="text-danger"></span>
            </div>

            <div class="form-group d-flex justify-content-between">
                <a asp-action="ViewAssignments" asp-route-classId="@Model.ClassId" class="btn btn-secondary">Cancel</a>
                <input type="submit" value="Create Assignment" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}